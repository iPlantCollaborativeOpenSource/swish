description "badgekit admin"

start on started mountall
stop on shutdown

respawn
respawn limit 99 5

script
   . {{ ADMIN_VARIABLES_FILE }}
   exec /usr/local/bin/node {{ ADMIN_INSTALL_LOCATION }}/app/index.js >> {{ ADMIN_LOG_LOCATION }}
end script

post-start script

end script
